public class Constraint {

    public boolean isValid(Variable[][] variables, Variable variable, Integer domain) {
        int r = variable.row;
        int c = variable.column;
        // for (int i = 0; i < variables.length; i++) {
        //     if
        // }
        return true;
    }
}
